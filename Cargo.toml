[package]
name = "axo"
version = "0.1.0"
edition = "2021"

[features]
default = [
    "internal",
    "formation",
    "initial", "scanner",
    "parser", "resolver",
]
formation = []
internal = [
    "formation",
    "initial", "scanner",
    "parser", "resolver",
]
initial = ["formation", "scanner", "parser"]
scanner = ["formation"]
parser = ["formation", "scanner"]
resolver = ["scanner", "parser"]
generator = ["resolver", "inkwell"]

[dependencies]
broccli = { version = "0.0.22" }
log = { version = "0.4.27"}
inkwell = { version = "0.8.0", features = ["llvm19-1-prefer-dynamic"], optional = true }

[workspace]
members = [
    ".",
]
