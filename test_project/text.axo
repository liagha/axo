        x = 42;
        y = 3.14;
        z = true;

        a = x + y * 2;
        b = (x - y) / 2;
        c = x % 5;

        d = x > y;
        e = x <= y;

        f = z && (x == 42);
        g = !z || (y != 3.14);

        if (x > 10) {
            x = x - 10;
        } else {
            x = x + 10;
        }

        while (x < 10) {
            x = 10;
        }

        ch = 'a';
        str = "Hello, world!";

        range = 1..10;