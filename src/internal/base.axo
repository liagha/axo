enum Token(compiler) {}

struct Integer {
    compiler,
    const value: Token,
    const size: Token,
    const signed: Boolean,
}

struct Float {
    compiler,
    const value: Token,
    const size: Token,
}

struct Boolean {
    compiler,
    const value: Token,
}

func printf() {}

struct Add(const Left, const Right, const Output, compiler) {
}