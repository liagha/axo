enum Token {}

struct Integer {
    var value: Token,
    var size: Token,
    var signed: Boolean,
}

struct Float {
    var value: Token,
    var size: Token,
}

struct Boolean {
    var value: Token,
}
