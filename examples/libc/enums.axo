enum Color {
    const Red,
    const Yellow,

    struct GrayScale {
        const value: Integer
    }

    enum Blue {
        const Cyan,
        const LightBlue,
        const DarkBlue
    }
}

struct Point {
    const x: Integer, const y: Integer,
}

func main() {
    const point = Point { x = 4, y = 6 }
    const red = Color.Yellow

    const cyan = Color.Blue.Cyan

    const black = Color.GrayScale {
        value = 100
    }

    Point { const x, const y } = point

    x
    y
}
