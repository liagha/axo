enum Color {
    const Red,
    const Yellow,

    struct GrayScale {
        const value: Integer
    }

    enum Blue {
        const Cyan,
        const LightBlue,
        const DarkBlue
    }
}

func main() {
    const red = Color.Yellow

    const cyan = Color.Blue.Cyan

    const black = Color.GrayScale {
        value = 100
    }

    const Color.GrayScale { value } = black;

    value
}
