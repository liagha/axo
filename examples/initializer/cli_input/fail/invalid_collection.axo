struct Point {
    const x: Integer,
    const y: Integer,
}

enum Toggle {
    const On,
    const Off,
}

const origin: Point = Point { x = 0, y = 0 }
const coords: (Integer, Integer) = (origin.x, origin.y)

print(coords[0])
print(coords[1])
