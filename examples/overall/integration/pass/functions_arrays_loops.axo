func fib(const n: Integer): Integer {
    if(n <= 1, n, fib(n - 1) + fib(n - 2))
}

const xs: [Integer; 6] = [1, 2, 3, 4, 5, 6]

var idx = 0
var acc = 0
while(idx < 6, {
    acc = acc + xs[idx] * fib(idx)
    idx = idx + 1
})

print(acc)
